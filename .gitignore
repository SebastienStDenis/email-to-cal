.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets and runtime state — never commit these.
.env
data/
*.sqlite
*.sqlite-wal
*.sqlite-shm
credentials.json
token.json

# The stylesheet is built from styles/ and committed; its toolchain is not.
node_modules/

# Local overrides of the shipped compose file.
docker-compose.override.yml

# Claude Code session workspaces.
.claude/

# macOS Finder metadata.
.DS_Store

# Screenshots the Playwright browser writes.
.playwright-mcp/
